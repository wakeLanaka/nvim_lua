require'tabline'.setup {
  enable = true,
  options = {
    show_tabs_always = true,
    show_devicons = true,
    show_filename_only = true,
    modified_italic = true,
    show_tabs_only = true,
  }
}
